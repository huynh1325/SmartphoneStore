import styles from './RetryVerify.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

const RetryVerify = () => {
    return (
        <div>

        </div>
    )
}

export default RetryVerify;